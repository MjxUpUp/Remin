# Memory 项目宪法（PRINCIPLES v1.0）

> 本文档是项目的最高决策依据。任何功能设计、技术选型、商业合作、PR 合并，与本文冲突时以本文为准。
> 修订需像宪法一样对待：明确提案、说明影响、显式版本号。

四条原则的口号：

1. **核心不认识任何 agent**（Agent 中立）
2. **换脑不换忆**（无差异迁移）
3. **宁可不知道，不能自信地错**（极高准确性）
4. **记忆比智能活得久**（多智能可复用）

冲突时的优先序：**P3 准确性 > P1 中立性 ≈ P2 迁移性 > P4 可复用性 >（其下才是）便利性 / 成本 / 延迟 / 增长**。

---

## P1 · Agent 中立：核心不认识任何 agent

**意图**：不绑定任何 agent 或 harness。绑定的代价已被市场反复验证——绑 Cursor 的记忆功能被官方下线且数据删不干净；绑 Claude Code 记忆的能力正被 Anthropic 官方收编（Auto-Dream / Managed Agents Memory）；Letta 绑自家 harness 后被迫 pivot。依附单一平台的记忆产品没有明天。

**架构不变量**

- N1 核心零依赖：记忆格式、协议、存储、检索、信任层的代码**不允许 import 任何 agent 专属 SDK/API**。CI 强制检查。
- N2 适配器是边缘件：一切 agent 接入物（hooks 脚本、TS 胶壳、插件包装、视图导出器）可整体删除而核心无损。预算上限：全部适配器代码 ≤ 总代码量 20%，单一适配器 ≤ 8%。超预算即触发「该适配器是否在劫持核心」的架构评审。
- N3 协议先行：任何新能力必须先在语言中立协议层（MCP / CLI / REST）可用，适配器只做薄封装。**禁止「仅在某 agent 内可用」的功能**。
- N4 商业条款同守：不接受要求核心为特定 agent 定制行为、排他或优先的合作条款。

**禁令**

- 禁止把任何 agent 的事件模型、会话语义写进核心数据结构（核心只认我们自己的记忆事件协议）。
- 禁止为了某个 agent 的便利优化而牺牲协议通用性。
- 禁止深度依赖任何 harness 的非文档化内部 API（DSH 0.x 教训：每周破坏性变更，插件验证 551 过 / 351 挂）。

**验收**

- CI：核心包依赖扫描（零 agent SDK）。
- 「拔适配器」演练：删除任一适配器目录，核心测试全绿。
- 适配器预算仪表盘进周报。

**对既有计划的修正**：DSH / Claude Code 适配器策略保留，但一律降级为边缘件——默认走 MCP 通道（各家的文档级公开接口，最抗变动），TS 胶壳最小化（事件桥 + 声明，仅此而已）。

---

## P2 · 无差异迁移：换脑不换忆

**意图**：用户切换任何 agent（Claude Code → Codex → Cursor → DSH → 未来任何东西）时，记忆体验零差异。针对的痛点是社区原话「同一个项目活在三个脑子里（three heads）」——指令层已被 AGENTS.md 解决，记忆层无人解决，这就是我们的位置。

**架构不变量**

- H1 唯一真源：记忆只存一份 canonical store；对每个 agent 只提供**视图**（AGENTS.md / CLAUDE.md / GEMINI.md / MCP 响应），视图是真源的确定性投影，可随时删除重建。
- H2 检索主权在我们：记忆的检索、排序、注入内容由我们的核心执行，**不依赖 agent 端模型能力**——否则换个弱模型体验就变，违反零差异。
- H3 身份归用户：记忆归属用户/组织，不归属 agent。身份锚定在用户侧（本地主体/账户），不是任何 agent 的 user_id。
- H4 导出即完整：任意时刻一键导出全量（含 supersession 历史与 provenance），导入无损还原。格式自包含（markdown + frontmatter + git，无二进制黑盒）。**没有 export 的记忆系统等于数据人质**（Claude Code 官方记忆至今无 export 机制——这就是我们的对照面）。
- H5 内容对等，而非机制对等：各 agent 注入机制不同（200 行索引 vs MCP 工具调用）是允许的，但**注入的记忆内容必须等价**。
- H6 确定性检索：同版本索引 + 同查询 → 同结果。禁用把模型端随机 rerank 作为主检索路径（那是不可复现性和差异感的来源）。嵌入索引只允许作为可重建的加速层。

**验收**

- Parity eval 进 CI：同一任务集在 ≥2 个 agent 上的注入内容 diff 为零（或语义等价率达标）。
- Export → 清空 → Import roundtrip 哈希一致。

**禁令**

- 禁止任何「仅本 agent 可见/可用」的记忆字段。
- 禁止把记忆写在 agent 私有存储里（`~/.claude/...`、SQLite 黑盒）作为真源。

---

## P3 · 极高准确性：宁可不知道，不能自信地错

**意图**：既定事实的准确性压倒一切。社区共识是「错的记忆比没有记忆更糟」「记忆里最毒的不是遗忘，是看着像记忆的幻觉」；Mem0 的 time-blindness（新旧事实并存）、graphiti 的 41% 误失效，都是反面教材。

**准确的工程定义（重要）**：我们**无法承诺所有记忆都为真**（源头信息本身可能错），我们承诺的是「**绝不自信地错**」——每条记忆可溯源、冲突显式消解、未验证如实标注、不确定就弃权。准确性通过**可验证性**实现，而非通过幻觉式自信。

**架构不变量**

- A1 无来源不落库：provenance 必填（来源、时间、上下文），CI 层面 provenance 覆盖率 = 100%。
- A2 冲突不共存：新事实 supersede 旧事实必须显式（写 `supersedes:`，旧条目退出检索但保留 git 历史）。**禁止静默覆盖、禁止新旧并存**。
- A3 信任分层必标：`human-verified` / `agent-claimed` / `expired-unverified`，注入时分层信息随行——agent 必须知道自己吃到的是哪种记忆（用户实测：用户指令与记忆冲突时记忆 3/3 让位，权威必须外置并明示）。
- A4 verify-condition：可失效的记忆（「API 用的是 Postgres」「登录走 OAuth」）必须带失效条件，使用前对照外部事实源（如代码库）做 JIT 验证（GitHub Copilot 官方已验证此设计：+7% PR merge rate）。
- A5 弃权优于编造：检索置信不足时返回「不知道 / 需确认」，绝不返回模糊记忆让 agent 编。评测里 abstention 是一等能力（LongMemEval 五能力之一）。
- A6 原件不可变：原始捕获永不被摘要覆盖；一切摘要/派生物可从原件重放重建（这是审计与纠错的根基）。
- A7 冲突裁决永赢：准确性与覆盖率/成本/延迟/便利冲突时，准确性永远赢（见总优先序）。

**分级自治**：准确性不等于死板。写入自动化程度分档（全人工审收 → 低风险自动/高风险人工 → 全自动），用户可选；**但无论哪档，trust-tier 与 provenance 永远如实标注**——自动化程度是用户的选择，如实标注是系统的义务。

**验收**

- Conflict regression 进 CI：构造「Ronaldo→Messi」类事实变更集，旧事实注入率 = 0。
- Stale 注入率 = 0（verify-condition 触发后的过期事实不得进入注入集）。
- Provenance audit：任取一条注入记忆，可一键回答「谁、何时、从哪、什么状态」。

---

## P4 · 多智能可复用：记忆比智能活得久

**意图**：记忆是随用户/组织迁移的**资产**，不随智能体形态漂移——无论消费者是离身智能（coding agent、助理、多 agent 团队）还是具身智能（机器人、IoT），换模型、换 harness、换载体，记忆的语义与价值不损失、不偏差。理论依据：Letta 的判断（token 空间记忆是跨模型世代增值的资产，权重记忆随模型贬值）与参数化记忆的 catastrophic forgetting（Titans/WISE 距实用尚远）——我们押「资产层」，不押「模型层」（那是 Engram 和 Google 的战场）。

**架构不变量**

- R1 语义自包含：记忆不依赖特定模型的 tokenizer / embedding / 推理能力即可完整解读。真源 = 文本 + 显式结构；嵌入索引只是可丢弃可重建的加速层（P2-H6 同源）。
- R2 类型系统面向通用认知记忆全集：episodic（情景）/ semantic（事实）/ procedural（技能·教训）/ preference（偏好）/ decision（决策）/ spatial-context（空间·情境，为具身预留）。**MVP 只实现 coding 子集，但 schema 第一天就定义全集**——类型系统是北极星，不是范围扩张令。
- R3 派生可再生成：换更强的模型后，可对原件重新提炼（更高质量的摘要/关联），且再生成物永不回写真源（对应 A6）。
- R4 评测模型无关：记忆效果评测不绑定单一 judge 模型——用规则可判定任务或多 judge 交叉（行业基准信任危机的直接教训：换个 judge 分数差 20 个点）。
- R5 消费者无假设：记忆协议（MCP / CLI / REST）对调用方零假设——今天接 coding agent，明天接机器人控制器，协议不动。

**验收**

- 「换模型重放」测试：用两个不同家族的模型消费同一记忆库，任务表现差异在阈值内。
- Schema 全集存在性检查：具身/多智能类型（spatial-context 等）在 schema 中有定义（哪怕尚无实现）。

---

## 张力与裁决（预设判例）

| 张力 | 裁决 |
|---|---|
| P1 中立 vs 某个生态的渠道红利（如 DSH 爆发） | 允许做适配器，按 N2 预算 + N3 协议先行执行；渠道再热也不给核心加专供逻辑 |
| P3 准确 vs 自动化便利 | 分级自治：用户可调自动度，但标注与溯源义务不可关闭 |
| P3 准确 vs 覆盖率/成本 | 准确赢。少记可以，错记不行 |
| P2 零差异 vs 各 agent 机制不同 | 内容对等（H5），不强求机制对等 |
| P4 通用类型 vs 小而深聚焦 | schema 定义全集、实现只做子集；P4 不构成当前范围扩张的理由 |
| 增长/融资压力 vs 任一原则 | 原则赢。违背本宪法的增长是负债（参考：厂商自测自评的基准反噬、开源自托管残缺的口碑反噬） |

## 执行机制

1. **PR 检查表（五问）**，进 PR 模板：
   - 这个改动是否让核心依赖了任何 agent？（P1）
   - 换一个 agent，用户体验会产生差异吗？（P2）
   - 是否引入了无来源 / 无分层 / 静默覆盖的记忆路径？（P3）
   - 这个能力是否必须依赖特定模型才能解读或使用？（P4）
   - 准确性与便利冲突时，是否选了准确性？（优先序）
2. **宪法测试进 CI**：parity eval（跨 agent 内容对等）、provenance audit（无来源即 fail）、conflict regression（supersession 后旧事实不可注入）、export/import roundtrip。
3. **适配器预算仪表盘**：各适配器代码占比与维护成本周报可见。
4. **宪法修订流程**：提案 → 影响说明 → 显式版本号 bump，不允许悄悄改。

---

### 附：每条原则的证据索引（来自 2026-09 调研）

- P1：Letta 弃 memory 平台转型；Cursor Memories 官方下线且旧数据删不净；Anthropic 收编记忆（Auto-Dream/Dreams/Managed Agents Memory）+ 拒绝 AGENTS.md 原生支持（2000+ 票 issue）；DSH 0.x 每周 breaking changes。
- P2：r/cursor「three heads」原帖；AGENTS.md 成 30+ 工具事实标准而记忆层无人管；Claude Code 记忆无 export；ChatGPT→Claude 记忆迁移教程流行（需求真实）。
- P3：Mem0 #5352 time-blindness / #5867 双存；graphiti #1728 41% 误失效；「wrong memories worse than none」社区共识；LongMemEval 含 abstention 能力；MemoryAgentBench 冲突消解是全行业硬伤；HN 实测「maintenance/provenance/invalidation 才是未解问题」。
- P4：Letta token 空间记忆资产论；参数化记忆 catastrophic forgetting（WISE/Titans 距生产远）；Engram $98M 押模型层——差异化要求我们押资产层。
